import React from 'react';
import ReactDOM  from 'react-dom';
import { VisNetwork as App } from './App';

import './App.scss';

const element = document.getElementById("app");

ReactDOM.render(<App />, element);
